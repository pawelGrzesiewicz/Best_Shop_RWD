{
  "name": "sass",
  "version": "2.0.0",
  "source": "./index.html",
  "scripts": {
    "start": "parcel --log-level verbose",
    "build": "NODE_OPTIONS='--max_old_space_size=4096' parcel build --log-level verbose"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.6.0",
    "parcel": "^2.6.0"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
